
@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>404page</title>
    <link href="~/Content/bootstrap-3.3.7/css/bootstrap.min.css" rel="stylesheet" />
    <script src="~/Content/JQuery/jquery-3.4.0.js"></script>
    <script src="~/Content/bootstrap-3.3.7/js/bootstrap.min.js"></script>
    <style>
        html {
            margin: 0;
            padding: 0;
            background-color: white;
        }

        body,
        html {
            width: 100%;
            height: 100%;
            overflow: hidden;
        }

        #svgContainer {
            width: 640px;
            height: 512px;
            background-color: white;
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            margin: auto;
        }
    </style>
</head>
<body>
    <script type="text/javascript" src="~/Content/404js/bodymovin.js"></script>
    <script type="text/javascript" src="~/Content/404js/data.js"></script>
    <script type="text/javascript">
        function countDown(secs) {
            var jumpTo = document.getElementById('jumpTo');
            jumpTo.innerHTML = secs;
            if (secs > 0) {
                setTimeout("countDown(" + secs + ",'" + history.go(-1) + "')", 1000);
            }
            else {
                history.go(-1);
            }
        }
    </script>
    <div id="svgContainer"></div>

    <script type="text/javascript">
        var svgContainer = document.getElementById('svgContainer');
        var animItem = bodymovin.loadAnimation({
            wrapper: svgContainer,
            animType: 'svg',
            loop: true,
            animationData: JSON.parse(animationData)
        });
    </script>
    <p class="text-center text-danger" style="font-size:80px"><b>404</b></p>
    <h2 class="text-center"><b><span id="Time" class="text-warning">5</span>秒后自动跳转到上一页</b></h2>
    <script type="text/javascript">countDown(5);</script>
    <script>
        //使用匿名函数方法
        function countDown() {
            var time = document.getElementById("Time");
            if (time.innerHTML == 0) {
                history.go(-1);
            } else {
                time.innerHTML = time.innerHTML - 1;
            }
        }
        window.setInterval("countDown()", 1000);
    </script>
</body>
</html>
